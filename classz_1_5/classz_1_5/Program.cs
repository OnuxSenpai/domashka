using System;

public class Numbers
{
    public static void Main()
    {
        var otv = (Math.Sin(5) + Math.Pow(1.75, 2)) / (3 * Math.Exp(Math.Cos(7)));

        Console.WriteLine(otv);
    }
}